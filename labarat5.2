using System;
using System.Text;

class Program
{
    public static void Main()
    {
        Console.OutputEncoding = Encoding.UTF8;

        Console.WriteLine("Інтерфейс\n");
        ISolid shape;

        Console.WriteLine("Пiрамiда: ");
        Console.WriteLine("Введiть довжину сторони основу пiрамiди:");
        double a = double.Parse(Console.ReadLine());
        Console.WriteLine("Введiть висоту пiрамiди:");
        double h = double.Parse(Console.ReadLine());

        shape = new Pyramid(a, h);
        Console.WriteLine("Периметр: " + shape.GetVolume() + "\n");

        Console.WriteLine("Куля: ");
        Console.WriteLine("Введiть радiус кулi:");
        double r = double.Parse(Console.ReadLine());


        shape = new Sphere(r);
        Console.WriteLine("Площа: " + shape.GetVolume() + "\n");

        Console.ReadKey();
    }
}
public interface ISolid
{
    public double GetVolume();
}
public class Pyramid : ISolid
{
    public double A;
    public double H;
    public Pyramid(double a, double h)
    {
        A = a;
        H = h;
    }
    public double GetVolume()
    {
        return Math.Round((1.0 / 3.0) * Math.Pow(A, 2) * H, 3);
    }
}
public class Sphere : ISolid
{
    public double R;

    public Sphere(double r)
    {
        R = r;
    }
    public double GetVolume()
    {
        return Math.Round((4 / 3) * Math.Pow(R, 3), 3);
    }
}
